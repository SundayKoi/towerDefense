import type { ShopUpgradeDef, SaveData } from '@/types';

// Meta-progression purchases. Protocols earned persist across runs.
// Effects write to save.metaBoosts which createRun reads when seeding a new RunState.

export const SHOP_UPGRADES: ShopUpgradeDef[] = [
  // ── COMBAT ───────────────────────────────────────────────────────────────
  {
    id: 'adaptive_weapons',
    name: 'ADAPTIVE WEAPONS',
    description: 'Every run begins with +2% global damage. Stacks up to 20x.',
    icon: '▲',
    baseCost: 30,
    stackStep: 20,
    maxStacks: 20,
    category: 'power',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.globalDamagePct = stacks * 0.02; },
  },
  {
    id: 'neural_booster',
    name: 'NEURAL BOOSTER',
    description: 'Start each run at level +1, entering with more card options.',
    icon: '∆',
    baseCost: 80,
    stackStep: 120,
    maxStacks: 5,
    category: 'power',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingLevel = stacks; },
  },
  {
    id: 'critical_matrix',
    name: 'CRITICAL MATRIX',
    description: 'All towers gain +2% crit chance per run. Stacks up to 15x.',
    icon: '◈',
    baseCost: 40,
    stackStep: 30,
    maxStacks: 15,
    category: 'power',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.globalCritChancePct = stacks * 0.02; },
  },
  {
    id: 'overclocked',
    name: 'OVERCLOCKED CORE',
    description: 'All towers fire +3% faster per run. Stacks up to 10x.',
    icon: '⚡',
    baseCost: 50,
    stackStep: 40,
    maxStacks: 10,
    category: 'power',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.globalRatePct = stacks * 0.03; },
  },
  {
    id: 'extended_sensors',
    name: 'EXTENDED SENSORS',
    description: 'All towers gain +2% range per run. Stacks up to 10x.',
    icon: '◎',
    baseCost: 45,
    stackStep: 35,
    maxStacks: 10,
    category: 'power',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.globalRangePct = stacks * 0.02; },
  },
  {
    id: 'hardened_core',
    name: 'HARDENED CORE',
    description: 'Start each run with +15 bonus HP. Stacks up to 5x.',
    icon: '♦',
    baseCost: 60,
    stackStep: 80,
    maxStacks: 5,
    category: 'power',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.bonusStartingHp = stacks * 15; },
  },

  // ── LOADOUT ───────────────────────────────────────────────────────────────
  {
    id: 'starter_firewall',
    name: 'STARTER: FIREWALL',
    description: 'Begin each run with +1 FIREWALL deploy token.',
    icon: '■',
    baseCost: 75,
    stackStep: 100,
    maxStacks: 3,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.firewall = stacks; },
  },
  {
    id: 'starter_honeypot',
    name: 'STARTER: HONEYPOT',
    description: 'Begin each run with +1 HONEYPOT deploy token.',
    icon: '⬡',
    baseCost: 90,
    stackStep: 120,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.honeypot = stacks; },
  },
  {
    id: 'starter_antivirus',
    name: 'STARTER: ANTIVIRUS',
    description: 'Begin each run with +1 ANTIVIRUS deploy token.',
    icon: '⊕',
    baseCost: 90,
    stackStep: 120,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.antivirus = stacks; },
  },
  {
    id: 'starter_ice',
    name: 'STARTER: ICE TOWER',
    description: 'Begin each run with +1 ICE TOWER deploy token.',
    icon: '❄',
    baseCost: 90,
    stackStep: 120,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.ice = stacks; },
  },
  {
    id: 'starter_chain',
    name: 'STARTER: CHAIN GUN',
    description: 'Begin each run with +1 CHAIN GUN deploy token.',
    icon: '⛓',
    baseCost: 100,
    stackStep: 130,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.chain = stacks; },
  },
  {
    id: 'starter_mine',
    name: 'STARTER: MINELAYER',
    description: 'Begin each run with +1 MINELAYER deploy token.',
    icon: '✕',
    baseCost: 100,
    stackStep: 130,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.mine = stacks; },
  },
  {
    id: 'starter_quantum',
    name: 'STARTER: QUANTUM NODE',
    description: 'Begin each run with +1 QUANTUM NODE deploy token.',
    icon: '◬',
    baseCost: 120,
    stackStep: 150,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.quantum = stacks; },
  },
  {
    id: 'starter_railgun',
    name: 'STARTER: RAILGUN',
    description: 'Begin each run with +1 RAILGUN deploy token.',
    icon: '⟹',
    baseCost: 130,
    stackStep: 160,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.railgun = stacks; },
  },
  {
    id: 'starter_pulse',
    name: 'STARTER: EMP ARRAY',
    description: 'Begin each run with +1 EMP ARRAY deploy token.',
    icon: '◉',
    baseCost: 120,
    stackStep: 150,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.pulse = stacks; },
  },
  {
    id: 'starter_sniper',
    name: 'STARTER: OVERWATCH',
    description: 'Begin each run with +1 OVERWATCH deploy token.',
    icon: '⊙',
    baseCost: 140,
    stackStep: 170,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.sniper = stacks; },
  },
  {
    id: 'starter_scrambler',
    name: 'STARTER: DISRUPTOR',
    description: 'Begin each run with +1 DISRUPTOR deploy token.',
    icon: '≋',
    baseCost: 110,
    stackStep: 140,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.scrambler = stacks; },
  },
  {
    id: 'starter_sentinel',
    name: 'STARTER: SENTINEL',
    description: 'Begin each run with +1 SENTINEL NODE deploy token.',
    icon: '⬡',
    baseCost: 110,
    stackStep: 140,
    maxStacks: 2,
    category: 'loadout',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.startingDeployTokens.sentinel = stacks; },
  },

  // ── ECONOMY ───────────────────────────────────────────────────────────────
  {
    id: 'protocol_audit',
    name: 'PROTOCOL AUDIT',
    description: 'Gain +5% XP from kills. Stacks up to 10x.',
    icon: '▣',
    baseCost: 50,
    stackStep: 40,
    maxStacks: 10,
    category: 'economy',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.xpBoostPct = stacks * 0.05; },
  },
  {
    id: 'data_harvest',
    name: 'DATA HARVEST',
    description: 'Earn +1 bonus Protocol each wave cleared. Stacks up to 5x.',
    icon: '◆',
    baseCost: 80,
    stackStep: 70,
    maxStacks: 5,
    category: 'economy',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.bonusProtocolsPerWave = stacks; },
  },

  // ── UTILITY ───────────────────────────────────────────────────────────────
  {
    id: 'card_bandwidth',
    name: 'CARD BANDWIDTH',
    description: 'Draw +1 card option per draft. Stacks up to 2x.',
    icon: '●●',
    baseCost: 200,
    stackStep: 400,
    maxStacks: 2,
    category: 'utility',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.extraDraftCards = stacks; },
  },
  {
    id: 'reroll_cache',
    name: 'REROLL CACHE',
    description: 'Start each draft with +1 reroll available. Stacks up to 4x.',
    icon: '↻',
    baseCost: 60,
    stackStep: 70,
    maxStacks: 4,
    category: 'utility',
    effect: (save: SaveData, stacks: number) => { save.metaBoosts.extraRerolls = stacks; },
  },
];

export const SHOP_BY_ID: Record<string, ShopUpgradeDef> = Object.fromEntries(SHOP_UPGRADES.map((u) => [u.id, u]));

export function shopCost(u: ShopUpgradeDef, nextStack: number): number {
  // Cost for purchasing the Nth stack (1-indexed): baseCost for 1st, + stackStep per additional
  return u.baseCost + (nextStack - 1) * u.stackStep;
}

// Recompute metaBoosts from scratch based on shopPurchased map. Call after any purchase.
export function recomputeMetaBoosts(save: SaveData): void {
  save.metaBoosts = {
    globalDamagePct: 0,
    globalCritChancePct: 0,
    globalRatePct: 0,
    globalRangePct: 0,
    bonusStartingHp: 0,
    bonusProtocolsPerWave: 0,
    startingLevel: 0,
    extraDraftCards: 0,
    extraRerolls: 0,
    xpBoostPct: 0,
    startingDeployTokens: {},
  };
  for (const u of SHOP_UPGRADES) {
    const stacks = save.shopPurchased[u.id] ?? 0;
    if (stacks > 0) u.effect(save, stacks);
  }
}
